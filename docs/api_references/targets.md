# Targets

:::patcherex2.targets.target

:::patcherex2.targets.builder

Concrete target declarations are exported from `patcherex2.targets`. Callers select
one explicitly when opening a `PatchSession`.
